# 🐟 带薪摸鱼 (FishWhileWorking)

一款趣味「带薪摸鱼」微信小程序，帮你实时计算上班摸鱼赚了多少钱，还能在公共鱼缸里养自己画的鱼。

## 功能一览

### 首页 — 薪资实时计算
- 仪表盘实时显示距离下班的倒计时
- 精确到秒的「今日已挣」金额
- 支持日薪/周薪/半月薪/月薪多种薪资类型
- 自定义上下班时间、午休时间、休息日
- 带薪摸鱼卡片：喝杯咖啡赚了多少、上个厕所赚了多少
- 倒数日卡片：距离发薪日、周末、节假日还有几天

### 鱼缸 — 公共鱼缸 (WebGL)
- WebGL 渲染的鱼缸场景，鱼自由游动、尾巴摆动
- 点击鱼可以点赞，点赞越多的鱼越大（最小 50px → 最大 120px）
- 每人每条鱼限赞 1 次，再点取消
- 鱼之间不会重叠，自动碰撞推开
- 点击鱼时鱼会停下来，方便操作

### 画鱼 — 自由创作
- 画笔 / 填充 / 橡皮擦工具
- 12 种颜色 + 3 种画笔粗细
- 撤销、清空功能
- 保存后自动裁剪上传到云端，投放到公共鱼缸

### 个人中心
- 微信一键登录
- 查看/管理我画的鱼
- 积分商店（开发中）

## 技术栈

| 层级 | 技术 |
|------|------|
| 框架 | uni-app 3.x + Vue 3.4 |
| 构建 | Vite 5 |
| 语言 | TypeScript + JavaScript |
| 渲染 | WebGL (鱼缸) + Canvas 2D (画鱼) |
| 后端 | 微信云开发（云函数 + 云数据库 + 云存储） |
| 样式 | SCSS + rpx 响应式 |

## 项目结构

```
├── cloudfunctions/          # 微信云函数
│   ├── login/               # 用户登录
│   ├── saveFish/            # 鱼的 CRUD + 点赞
│   ├── saveSettings/        # 工作设置同步
│   ├── syncFishRecords/     # 鱼记录同步
│   └── updateProfile/       # 更新用户资料
├── src/
│   ├── components/          # 公共组件
│   │   ├── CustomTabBar.vue # 自定义底部导航
│   │   ├── GaugeChart.vue   # 仪表盘组件
│   │   ├── FishingCard.vue  # 摸鱼卡片
│   │   └── CountdownCard.vue# 倒数日卡片
│   ├── pages/
│   │   ├── index/           # 首页（薪资计算）
│   │   ├── fish-tank/       # 鱼缸（WebGL）
│   │   ├── draw-fish/       # 画鱼
│   │   ├── my-fish/         # 我的鱼
│   │   ├── profile/         # 个人中心
│   │   └── ...
│   ├── store/               # 状态管理
│   ├── utils/               # 工具函数
│   └── static/              # 静态资源
├── package.json
└── vite.config.ts
```

## 快速开始

```bash
# 安装依赖
pnpm install

# 微信小程序开发模式
pnpm run dev:mp-weixin

# 微信小程序生产构建
pnpm run build:mp-weixin
```

构建完成后，用微信开发者工具导入 `dist/dev/mp-weixin`（开发）或 `dist/build/mp-weixin`（生产）目录即可运行。

## 云开发配置

1. 在微信开发者工具中开通云开发
2. 创建以下数据库集合：
   - `fishes` — 鱼数据（fileID, openid, likes, createdAt）
   - `fish_likes` — 点赞记录（fishId, openid, createdAt）
   - `settings` — 用户工作设置
   - `profiles` — 用户资料
3. 上传 `cloudfunctions/` 下的所有云函数

## 截图预览

> 待补充

## License

MIT
